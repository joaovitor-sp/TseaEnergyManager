<UserControl x:Class="Tsea.Desktop.Views.StatsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="250">
    <Border Background="#1e3c72" CornerRadius="8" Padding="10">
        <StackPanel>
            <TextBlock Text="Estatísticas (ViewModel Isolado)" Foreground="White" FontWeight="Bold" FontSize="12" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding TotalEquipments}" Foreground="#e0e8f5" FontSize="12" Margin="0,0,0,10" />
            <Button Content="Atualizar Estatísticas" Command="{Binding RefreshStatsCommand}" Padding="5" />
        </StackPanel>
    </Border>
</UserControl>
